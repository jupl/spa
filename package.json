{
  "description": " ",
  "repository": "git",
  "dependencies": {
    "del": "^0.1.1",
    "gulp": "^3.8.2",
    "gulp-autoprefixer": "^0.0.7",
    "gulp-bower": "^0.0.6",
    "gulp-csso": "^0.2.9",
    "gulp-less": "^1.2.4",
    "gulp-load-plugins": "^0.5.2",
    "gulp-util": "^2.2.19",
    "webpack": "^1.3.1"
  },
  "devDependencies": {
    "browser-sync": "^1.1.1",
    "karma-detect-browsers": "^0.1.2",
    "karma-chai-plugins": "^0.2.1",
    "karma-mocha": "^0.1.4",
    "karma-webpack": "^1.2.1",
    "sugar": "^1.4.1"
  },
  "scripts": {
    "start": "npm install --production && npm run server:prod",
    "postinstall": "npm run bower",
    "bower": "gulp bower",
    "build:dev": "npm run bower && gulp build",
    "build:prod": "NODE_ENV=production npm run build:dev",
    "watch:dev": "npm run bower && gulp watch",
    "watch:prod": "NODE_ENV=production npm run watch:dev",
    "server:dev": "npm run bower && gulp server",
    "server:prod": "NODE_ENV=production npm run server:dev",
    "unit:test": "gulp unit:test",
    "unit:tdd": "gulp unit:tdd",
    "unit:tdd:server": "gulp unit:tdd:server"
  }
}
